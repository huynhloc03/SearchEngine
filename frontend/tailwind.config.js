/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',         // Include the main HTML file
    './src/**/*.{js,jsx}',  // Include all JSX/JS files inside the 'src' folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
